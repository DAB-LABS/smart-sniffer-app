#include <tunables/global>

profile smart_sniffer_agent flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability sys_rawio,
  capability sys_admin,

  # Network
  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,

  # Broad read access (matches HA default pattern)
  / r,
  /** r,

  # Writable paths
  /data/** rw,
  /tmp/** rwk,
  /run/{,**} rwk,

  # s6-overlay init system
  /init rix,
  /run.sh rix,
  /package/** rix,
  /command/** rix,
  /etc/s6/** rix,
  /etc/s6-overlay/** rix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,

  # Executables
  /bin/** rix,
  /usr/bin/** rix,
  /usr/sbin/** rix,
  /usr/lib/bashio/** rix,
  /opt/mock-agent.py rix,

  # Libraries (memory-mapped)
  /lib/** mr,
  /usr/lib/** mr,
  /usr/local/lib/** mr,

  # Device access for smartctl
  /dev/sd* r,
  /dev/nvme* r,
  /dev/vd* r,
  /dev/sg* r,
  /dev/null rw,
  /dev/urandom r,
  /dev/random r,
  /dev/tty rw,
}
